<div class="new-rental-ticket-container">
  <%= form_tag rental_rental_tickets_path(@rental) do %>
    <article>
      <% if flash[:error].present? %>
        <div id="error_explanation">
          <p><%= flash[:error] %></p>
        </div>
      <% end %>
      <label class="amount">
        <span>Amount: $<%=@rental.cost%></span>
      </label>
    </article>

    <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
            data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
            data-description="Purchase Rental"
            data-amount="<%=@rental.cost.to_i*100%>"
            data-locale="auto"></script>
  <% end %>
</div>
